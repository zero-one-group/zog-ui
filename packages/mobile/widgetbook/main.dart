import 'package:flutter/material.dart';

import 'widgetbook.dart';

/// how to run this example:
/// `flutter run -t widgetbook/main.dart -d chrome`
/// or
/// `flutter run -t widgetbook/main.dart -d [specific device]`
///

void main() {
  runApp(const HotReload());
}
