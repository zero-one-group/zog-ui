/// List types of [ZeroNavigationRailType]
///
/// This type use for [ZeroNavigationRail] component
enum ZeroNavigationRailType {
  /// [ZeroNavigationRail] icon and label always appears when state is active or inactive
  iconLabel,

  /// [ZeroNavigationRail] label appear when state is active
  ///
  /// For icon, still always appear
  iconLabelOnActive,
}
