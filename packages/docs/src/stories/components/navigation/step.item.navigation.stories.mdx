import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { BADGE } from '@geometricpanda/storybook-addon-badges';
import { within, userEvent } from '@storybook/testing-library';
import { expect } from '@storybook/jest';
import { StepItemNavigation } from '@zero-ui/core';
import { MinusCircleFilled } from '@ant-design/icons';

<Meta
  title="Components/Navigation/StepItem"
  component={[StepItemNavigation]}
  argTypes={{}}
/>

export const StepItemNavigationStory = (args) => (
  <StepItemNavigation {...args} />
);

# StepItemNavigation

StepItemNavigation

## Example Usage

```tsx
import { StepItemNavigation } from '@zero-ui/core';
```

<Canvas columns={2} withSource="close">
  <Story
    args={{
      label: 'Finished',
      description: 'This is a description.',
      state: 'stepCompleted',
      step: 1,
      size: 'small',
      icon: <MinusCircleFilled size={5} />,
    }}
    name="StepItemNavigation"
  >
    {StepItemNavigationStory.bind()}
  </Story>
</Canvas>

<ArgsTable of={StepItemNavigation} />
