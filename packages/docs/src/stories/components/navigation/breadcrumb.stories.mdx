import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { BADGE } from '@geometricpanda/storybook-addon-badges';
import { within, userEvent } from '@storybook/testing-library';
import { expect } from '@storybook/jest';
import { Breadcrumb } from '@01group-ui/core';
import { HomeOutlined } from '@ant-design/icons';

<Meta
  title="Components/Navigation/Breadcrumb"
  component={[Breadcrumb]}
  argTypes={{}}
/>

export const BreadcrumbStory = (args) => <Breadcrumb {...args} />;

# Breadcrumb

Breadcrumb

## Example Usage

```jsx
import { Breadcrumb } from '@01group-ui/core';
```

<Canvas columns={2} withSource="close">
  <Story
    args={{
      items: [
        { title: 'Mantine', href: '#', current: false },
        { title: 'Mantine hooks', href: '#', current: false },
        { title: 'use-id', href: '#', current: true },
      ],
      separator: '/',
    }}
    name="Breadcrumb"
  >
    {BreadcrumbStory.bind()}
  </Story>
</Canvas>

<ArgsTable of={Breadcrumb} />
