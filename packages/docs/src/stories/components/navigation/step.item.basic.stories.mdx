import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { BADGE } from '@geometricpanda/storybook-addon-badges';
import { within, userEvent } from '@storybook/testing-library';
import { expect } from '@storybook/jest';
import { StepItemBasic } from '@01group-ui/core';
import { MinusCircleFilled } from '@ant-design/icons';

<Meta
  title="Components/Navigation/StepItem"
  component={[StepItemBasic]}
  argTypes={{}}
/>

export const StepItemBasicStory = (args) => <StepItemBasic {...args} />;

# StepItemBasic

StepItemBasic

## Example Usage

```tsx
import { StepItemBasic } from '@01group-ui/core';
```

<Canvas columns={2} withSource="close">
  <Story
    args={{
      orientation: 'vertical',
      label: 'Finished',
      description: 'This is a description.',
      state: 'stepCompleted',
      step: 1,
      size: 'sm',
    }}
    name="StepItemBasic"
  >
    {StepItemBasicStory.bind()}
  </Story>
</Canvas>

<ArgsTable of={StepItemBasic} />
