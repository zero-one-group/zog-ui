import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { BADGE } from '@geometricpanda/storybook-addon-badges';
import { within, userEvent } from '@storybook/testing-library';
import { expect } from '@storybook/jest';
import { PopConfirm, Space, Button } from '@zog-ui/core';

<Meta
  title="Components/Portal/PopConfirm"
  component={[PopConfirm]}
  argTypes={{
    side: {
      type: 'radio',
      options: ['top', 'bottom', 'left', 'right'],
    },
  }}
/>

export const PopConfirmStory = (args) => (
  <Space justify="center" css={{ marginTop: '100px' }}>
    <PopConfirm {...args}></PopConfirm>
  </Space>
);

# PopConfirm

> Displays rich content in a portal, triggered by a button.

## Example Usage

```tsx
import { PopConfirm } from '@zog-ui/core';
```

<Canvas columns={2} withSource="close">
  <Story
    args={{
      title: 'Are you sure delete this task?',
      trigger: (
        <Button intent="primary" size="md" variant="default">
          Delete me!
        </Button>
      ),
      labelButtonLeft: 'No',
      labelButtonRight: 'Yes',
      onAction: () => alert('sukses delete'),
    }}
    name="PopConfirm"
  >
    {PopConfirmStory.bind()}
  </Story>
</Canvas>

<ArgsTable of={PopConfirm} />
