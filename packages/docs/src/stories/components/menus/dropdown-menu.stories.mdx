import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { BADGE } from '@geometricpanda/storybook-addon-badges';
import { within, userEvent } from '@storybook/testing-library';
import { expect } from '@storybook/jest';
import { DropdownMenu } from '@zero-ui/core';

<Meta title="Components/Menus/DropdownMenu" component={[DropdownMenu]} argTypes={{}} />

export const DropdownMenuStory = () => <DropdownMenu />;

# DropdownMenu

> WIP

## Example Usage

```jsx
import { DropdownMenu } from '@zero-ui/core';
```

<Canvas columns={2} withSource="close">
  <Story name="DropdownMenu">{DropdownMenuStory.bind()}</Story>
</Canvas>

<ArgsTable of={DropdownMenu} />
