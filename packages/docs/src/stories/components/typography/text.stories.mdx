import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { BADGE } from '@geometricpanda/storybook-addon-badges';
import { within, userEvent } from '@storybook/testing-library';
import { expect } from '@storybook/jest';
import { Text } from '@zog-ui/core';

<Meta title="Components/Typography/Text" component={[Text]} argTypes={{}} />

export const TextStory = (args) => <Text {...args} />;

# Text

Basic text writing, usually used as a body text.

## Example Usage

```jsx
import { Text } from '@zog-ui/core';
```

<Canvas columns={2} withSource="close">
  <Story
    name="Text"
    args={{
      children:
        'Lorem ipsum dolor sit amet, consectetur adipiscing elit. Proin non lectus eu nunc bibendum laoreet a sit amet augue.',
    }}
  >
    {TextStory.bind()}
  </Story>
</Canvas>

<ArgsTable of={Text} />
