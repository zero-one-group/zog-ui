import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { Heading } from '@zog-ui/core';

<Meta
  title="Components/Typography/Heading"
  component={[Heading]}
  argTypes={{}}
/>

export const HeadingStory = (args) => (
  <Heading {...args}>Hello Heading</Heading>
);

# Heading

Component that used to create heading element like a `h1`, `h2`, etc.

## Example Usage

```jsx
import { Heading } from '@zog-ui/core';
```

<Canvas columns={2} withSource="close">
  <Story
    name="Heading"
    args={{
      as: 'h2',
    }}
  >
    {HeadingStory.bind()}
  </Story>
</Canvas>

<ArgsTable of={Heading} />
