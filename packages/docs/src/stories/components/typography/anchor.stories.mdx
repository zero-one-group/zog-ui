import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { BADGE } from '@geometricpanda/storybook-addon-badges';
import { within, userEvent } from '@storybook/testing-library';
import { expect } from '@storybook/jest';
import { Anchor } from '@zog-ui/core';

<Meta title="Components/Typography/Anchor" component={[Anchor]} argTypes={{}} />

export const AnchorStory = (args) => <Anchor {...args}>{args.children}</Anchor>;

# Anchor

Basic text writing, usually used as a body text.

## Example Usage

```tsx
import { Anchor } from '@zog-ui/core';
```

<Canvas columns={2} withSource="close">
  <Story
    name="Anchor"
    args={{
      href: 'https://zero-one-group.com/technology/',
      target: '_blank',
      children: 'we are zero one technology',
    }}
  >
    {AnchorStory.bind()}
  </Story>
</Canvas>

<ArgsTable of={Anchor} />
