import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { BADGE } from '@geometricpanda/storybook-addon-badges';
import { within, userEvent } from '@storybook/testing-library';
import { expect } from '@storybook/jest';
import { Button } from '@zero-ui/core';

<Meta title="Components/Buttons/Button" component={[Button]} argTypes={{}} />

export const ButtonStory = (args) => <Button {...args}>Sample Button</Button>;

# Button

Buttons are elements requesting user action. They look simple but have numerous important details like primary, secondary, focused, disabled, accessible, form integration, and more.

## Example Usage

```jsx
import { Button } from '@zero-ui/core';
```

<Canvas columns={2} withSource="close">
  <Story name="Button">{ButtonStory.bind()}</Story>
</Canvas>

<ArgsTable of={Button} />
