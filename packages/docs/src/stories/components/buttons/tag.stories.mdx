import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { BADGE } from '@geometricpanda/storybook-addon-badges';
import { within, userEvent } from '@storybook/testing-library';
import { expect } from '@storybook/jest';
import { Button } from '@zog-ui/core';
import { CheckCircleOutlined, CloseCircleOutlined } from '@ant-design/icons';

<Meta
  title="Components/Buttons/Tag"
  argTypes={{
    variant: {
      options: ['dashed', 'default', 'outlined', 'circle', 'square'],
      control: { type: 'radio' },
    },
    size: {
      options: ['sm', 'md', 'lg'],
      control: { type: 'radio' },
    },
    intent: {
      options: ['default', 'primary', 'secondary', 'danger'],
      control: { type: 'radio' },
    },
    disabled: { control: 'boolean' },
    ghost: { control: 'boolean' },
  }}
  component={[Button]}
/>

export const TagStory = (args) => <Button {...args}>{args.children}</Button>;

# Button

Tag are elements requesting user action. They look simple but have numerous important details like primary, secondary, focused, disabled, accessible, form integration, and more.

## Example Usage

```jsx
import { Button } from '@zog-ui/core';
```

<Canvas columns={2} withSource="close">
  <Story
    args={{
      intent: 'default',
      variant: 'default',
      size: 'md',
      children: 'Simple Button',
      css: {
        color: '$gray12',
        backgroundColor: '$gray1',
        borderColor: '$gray4',
      },
      prefix: <CheckCircleOutlined />,
      suffix: <CloseCircleOutlined />,
    }}
    name="Tag"
  >
    {TagStory.bind()}
  </Story>
</Canvas>

<ArgsTable of={Button} />
