import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { BADGE } from '@geometricpanda/storybook-addon-badges';
import { within, userEvent } from '@storybook/testing-library';
import { expect } from '@storybook/jest';
import { IconButton } from '@zero-ui/core';

<Meta title="Components/Buttons/IconButton" component={[IconButton]} argTypes={{}} />

export const IconButtonStory = () => <IconButton />;

# IconButton

> WIP

## Example Usage

```jsx
import { IconButton } from '@zero-ui/core';
```

<Canvas columns={2} withSource="close">
  <Story name="IconButton">{IconButtonStory.bind()}</Story>
</Canvas>

<ArgsTable of={IconButton} />
