import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { BADGE } from '@geometricpanda/storybook-addon-badges';
import { within, userEvent } from '@storybook/testing-library';
import { expect } from '@storybook/jest';
import { Button, ButtonGroup } from '@zero-ui/core';

<Meta title="Components/Buttons/ButtonGroup" component={[ButtonGroup]} />

export const ButtonStory = (args) => (
  <ButtonGroup {...args}>{args.children}</ButtonGroup>
);

# ButtonGroup

ButtonsGroup are component as parent for button and the display is flex.

## Example Usage

```jsx
import { Button, ButtonGroup } from '@zero-ui/core';
```

<Canvas columns={2} withSource="close">
  <Story
    name="ButtonGroup"
    args={{
      children: (
        <>
          <Button intent="primary" size="lg">
            Button
          </Button>
          <Button intent="secondary" size="lg">
            Button
          </Button>
          <Button intent="secondary" size="lg">
            Button
          </Button>
        </>
      ),
    }}
  >
    {ButtonStory.bind()}
  </Story>
</Canvas>

<ArgsTable of={ButtonGroup} />
