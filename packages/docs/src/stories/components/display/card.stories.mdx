import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { BADGE } from '@geometricpanda/storybook-addon-badges';
import { within, userEvent } from '@storybook/testing-library';
import { expect } from '@storybook/jest';
import { Card } from '@zero-ui/core';
import {
  EditOutlined,
  EllipsisOutlined,
  SettingOutlined,
} from '@ant-design/icons';

<Meta title="Components/Display/Card" component={[Card]} argTypes={{}} />

export const CardStory = (args) => <Card {...args} />;

# Card

> Simple rectangular container.

<br />> A card can be used to display content related to a single subject. The
content can consist of multiple elements of varying types and sizes.

## Example Usage

```jsx
import { Card } from '@zero-ui/core';
```

<Canvas columns={2} withSource="close">
  <Story
    name="Card"
    args={{
      bordered: true,
      title: 'Card Title',
      extra: <a href="#">More</a>,
      children: <h1>This is Card!</h1>,
      cover: (
        <img
          alt="example"
          src="https://www.giantbomb.com/a/uploads/scale_small/3/33873/1700999-naruto.png"
        />
      ),
      css: {
        width: '270px',
      },
      actions: [
        {
          onClick: () => alert('Click'),
          component: <SettingOutlined key="setting" />,
        },
        {
          component: <EditOutlined key="edit" />,
        },
        {
          component: <EllipsisOutlined key="ellipsis" />,
        },
      ],
    }}
  >
    {CardStory.bind()}
  </Story>
</Canvas>

<ArgsTable of={Card} />
