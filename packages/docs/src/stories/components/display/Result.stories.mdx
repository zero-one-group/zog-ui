import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { BADGE } from '@geometricpanda/storybook-addon-badges';
import { within, userEvent } from '@storybook/testing-library';
import { expect } from '@storybook/jest';
import { Result, Button } from '@zog-ui/core';
import { InfoCircleOutlined } from '@ant-design/icons';

<Meta
  title="Components/Display/Result"
  component={[Result]}
  argTypes={{
    intent: {
      options: ['success', 'info', 'warning', 'error'],
      control: { type: 'radio' },
    },
  }}
/>

export const ResultStory = (args) => <Result {...args} />;

# Result

> A component Result is a component for showing information result after user submit something

## Example Usage

```tsx
import { Result } from '@zog-ui/core';
```

<Canvas columns={2} withSource="close">
  <Story
    args={{
      title: 'title',
      description: 'decription',
      extraDescription: 'Order number: 2017182818828182881 has been placed.',
      action: (
        <>
          <Button intent="primary" size="md" variant="default">
            Go Console
          </Button>{' '}
          <Button
            css={{ backgroundColor: 'white', borderColor: 'black' }}
            size="md"
            variant="default"
            intent="default"
          >
            Buy Again
          </Button>
        </>
      ),
      icon: (
        <InfoCircleOutlined style={{ fontSize: '70px', color: '#1890FF' }} />
      ),
    }}
    name="Result"
  >
    {ResultStory.bind()}
  </Story>
</Canvas>

<ArgsTable of={Result} />
