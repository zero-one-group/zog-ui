import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { BADGE } from '@geometricpanda/storybook-addon-badges';
import { within, userEvent } from '@storybook/testing-library';
import { expect } from '@storybook/jest';
import { VerifiedBadge } from '@zog-ui/core';

<Meta
  title="Components/Display/VerifiedBadge"
  component={[VerifiedBadge]}
  argTypes={{}}
/>

export const VerifiedBadgeStory = () => <VerifiedBadge />;

# VerifiedBadge

> WIP

## Example Usage

```jsx
import { VerifiedBadge } from '@zog-ui/core';
```

<Canvas columns={2} withSource="close">
  <Story name="VerifiedBadge">{VerifiedBadgeStory.bind()}</Story>
</Canvas>

<ArgsTable of={VerifiedBadge} />
