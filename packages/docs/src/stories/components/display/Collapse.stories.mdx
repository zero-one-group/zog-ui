import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { BADGE } from '@geometricpanda/storybook-addon-badges';
import { within, userEvent } from '@storybook/testing-library';
import { expect } from '@storybook/jest';
import { CollapseWrapper, Result, Button, Collapse } from '@zog-ui/core';

<Meta
  title="Components/Display/Collapse"
  component={[Collapse]}
  argTypes={{}}
/>

export const CollapseStory = (args) => (
  <>
    <Collapse header="This is Panel header 1" content="This is  content 1" />
    <Collapse header="This is panel header 2" content="This is content 2" />
    <Collapse header="This is panel header 3" content="This is content 3" />
  </>
);

# Collapse

> An interactive component which expands/collapses a panel.

## Example Usage

```tsx
import { Collapse } from '@zog-ui/core';
```

<Canvas columns={2} withSource="close">
  <Story name="Collapse">{CollapseStory.bind()}</Story>
</Canvas>

<ArgsTable of={Collapse} />
