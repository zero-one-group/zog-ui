import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { BADGE } from '@geometricpanda/storybook-addon-badges';
import { within, userEvent } from '@storybook/testing-library';
import { expect } from '@storybook/jest';
import { Scrollbar } from '@01group-ui/core';

<Meta title="Components/Display/Scrollbar" component={[Scrollbar]} argTypes={{}} />

export const ScrollbarStory = () => <Scrollbar />;

# Scrollbar

> WIP

## Example Usage

```jsx
import { Scrollbar } from '@01group-ui/core';
```

<Canvas columns={2} withSource="close">
  <Story name="Scrollbar">{ScrollbarStory.bind()}</Story>
</Canvas>

<ArgsTable of={Scrollbar} />
