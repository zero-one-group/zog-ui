import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { BADGE } from '@geometricpanda/storybook-addon-badges';
import { within, userEvent } from '@storybook/testing-library';
import { expect } from '@storybook/jest';
import { ProgressBar } from '@zero-ui/core';

<Meta title="Components/Display/ProgressBar" component={[ProgressBar]} argTypes={{}} />

export const ProgressBarStory = () => <ProgressBar />;

# ProgressBar

> WIP

## Example Usage

```jsx
import { ProgressBar } from '@zero-ui/core';
```

<Canvas columns={2} withSource="close">
  <Story name="ProgressBar">{ProgressBarStory.bind()}</Story>
</Canvas>

<ArgsTable of={ProgressBar} />
