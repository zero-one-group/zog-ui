import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { BADGE } from '@geometricpanda/storybook-addon-badges';
import { within, userEvent } from '@storybook/testing-library';
import { expect } from '@storybook/jest';
import { Carousel } from '@01group-ui/core';

<Meta
  title="Components/Display/Carousel"
  component={[Carousel]}
  argTypes={{
    dotsPosition: {
      options: ['top', 'bottom', 'left', 'right', undefined],
      control: { type: 'select' },
    },
  }}
/>

export const CarouselStory = ({ children, ...args }) => {
  const contentStyle = {
    height: 300,
    background: 'blue',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    color: 'white',
  };
  return (
    <Carousel {...args}>
      <div>
        <div style={contentStyle}>1</div>
      </div>
      <div>
        <div style={contentStyle}>2</div>
      </div>
      <div>
        <div style={contentStyle}>3</div>
      </div>
    </Carousel>
  );
};

# Carousel

> A carousel component. Scales with its container. Commonly used for a group of pictures/cards. Please make sure to wrap slider item in empty div

## Example Usage

```jsx
import { Carousel } from '@01group-ui/core';
```

<Canvas columns={2} withSource="close">
  <Story
    name="Carousel"
    args={{
      autoplay: true,
      infinite: true,
      fade: false,
      vertical: false,
      autoplaySpeed: 4000,
    }}
  >
    {CarouselStory.bind()}
  </Story>
</Canvas>

<ArgsTable of={Carousel} />
