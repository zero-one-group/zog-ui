import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { BADGE } from '@geometricpanda/storybook-addon-badges';
import { within, userEvent } from '@storybook/testing-library';
import { expect } from '@storybook/jest';
import { Alert } from '@zero-ui/core';
import { InfoCircleOutlined } from '@ant-design/icons';

<Meta
  title="Components/Display/Alert"
  component={[Alert]}
  argTypes={{
    intent: {
      options: ['success', 'info', 'warning', 'error'],
      control: { type: 'radio' },
    },
  }}
/>

export const AlertStory = (args) => <Alert {...args} />;

# Alert

> A component alert is a component for show like notification and variant like success,error, and warning

## Example Usage

```tsx
import { Alert } from '@zero-ui/core';
```

<Canvas columns={2} withSource="close">
  <Story
    args={{
      title: 'title',
      description: 'decription',
      extraContent: 'this is extra content',
      intent: 'success',
      onClose: () => console.log('ok'),
      icon: <InfoCircleOutlined />,
    }}
    name="Alert"
  >
    {AlertStory.bind()}
  </Story>
</Canvas>

<ArgsTable of={Alert} />
