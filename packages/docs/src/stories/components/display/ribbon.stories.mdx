import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { BADGE } from '@geometricpanda/storybook-addon-badges';
import { within, userEvent } from '@storybook/testing-library';
import { expect } from '@storybook/jest';
import { Ribbon, Box } from '@zog-ui/core';

<Meta
  title="Components/Display/Badge"
  component={[Ribbon]}
  argTypes={{
    placement: {
      type: 'radio',
      options: ['left', 'right'],
    },
  }}
/>

export const RibbonStory = (args) => (
  <Box
    css={{
      position: 'relative',
    }}
  >
    <Ribbon {...args} />
  </Box>
);

# Ribbon

> Ribbon is a graphical component that typically appears at the top of the application window

## Example Usage

```tsx
import { Ribbon } from '@zog-ui/core';
```

<Canvas columns={2} withSource="close">
  <Story
    args={{
      label: 'Ribbon',
    }}
    name="Ribbon"
  >
    {RibbonStory.bind()}
  </Story>
</Canvas>

<ArgsTable of={Ribbon} />
