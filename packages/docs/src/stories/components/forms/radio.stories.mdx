import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { BADGE } from '@geometricpanda/storybook-addon-badges';
import { within, userEvent } from '@storybook/testing-library';
import { expect } from '@storybook/jest';
import { Radio } from '@zog-ui/core';

<Meta title="Components/Forms/Radio" component={[Radio]} argTypes={{}} />

export const RadioStory = (args) => <Radio {...args} />;

# Radio

> A radio button is a graphical user interface element that allows a user to choose only one option from a predefined set of mutually exclusive options

## Example Usage

```tsx
import { Radio } from '@zog-ui/core';
```

<Canvas columns={2} withSource="close">
  <Story
    args={{
      name: 'list',
      id: 'list-radio',
      label: 'html',
      disabled: false,
    }}
    name="Radio"
  >
    {RadioStory.bind()}
  </Story>
</Canvas>

<ArgsTable of={Radio} />
