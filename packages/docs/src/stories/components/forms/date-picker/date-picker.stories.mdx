import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { DatePicker } from '@zero-ui/core';

<Meta
  title="Components/Forms/DatePicker/DatePicker"
  component={[DatePicker]}
  argTypes={{
    size: {
      options: ['small', 'medium', 'large'],
      control: { type: 'radio' },
    },
    colorScheme: {
      options: ['pink', 'slate', 'blue', 'teal'],
      control: { type: 'select' },
    },
    disabled: {
      control: { type: 'boolean' },
    },
  }}
/>

export const DatePickerStory = ({ children, ...args }) => (
  <div style={{ height: '100vh' }}>
    <DatePicker {...args}>{children}</DatePicker>
  </div>
);

# DatePicker

> DatePicker provides a visual way to select date and time

## Example Usage

```jsx
import { DatePicker } from '@zero-ui/core';
```

<Canvas columns={2} withSource="close">
  <Story
    name="DatePicker"
    args={{
      size: 'small',
      showTime: true,
      showToday: true,
      fullWidth: false,
    }}
  >
    {DatePickerStory.bind()}
  </Story>
</Canvas>

<ArgsTable of={DatePicker} />
