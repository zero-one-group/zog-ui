import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { DatePicker } from '@zero-ui/core';

<Meta
  title="Components/Forms/DatePicker/DatePicker"
  component={[DatePicker]}
  argTypes={{
    size: {
      options: ['small', 'medium', 'large'],
      control: { type: 'radio' },
    },
  }}
/>

export const DatePickerStory = ({ children, ...args }) => (
  <div style={{ height: '100vh' }}>
    <DatePicker {...args}>{children}</DatePicker>
  </div>
);

# DatePicker

> WIP

## Example Usage

```jsx
import { DatePicker } from '@zero-ui/core';
```

<Canvas columns={2} withSource="close">
  <Story
    name="DatePicker"
    args={{
      size: 'small',
      showTime: true,
      showToday: true,
    }}
  >
    {DatePickerStory.bind()}
  </Story>
</Canvas>

<ArgsTable of={DatePicker} />
