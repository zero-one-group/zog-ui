import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { BADGE } from '@geometricpanda/storybook-addon-badges';
import { within, userEvent } from '@storybook/testing-library';
import { expect } from '@storybook/jest';
import { RadioGroup, Radio } from '@zog-ui/core';

<Meta
  title="Components/Forms/Radio"
  component={[RadioGroup]}
  argTypes={{
    orientation: {
      options: ['horizontal', 'vertical'],
      control: 'radio',
    },
  }}
/>

export const RadioGroupStory = (args) => (
  <RadioGroup {...args}>
    <Radio defaultChecked label="html" id="html" name="tech" />
    <Radio label="css" id="css" name="tech" />
    <Radio label="web" id="web" name="tech" />
    <Radio label="javascript" id="js" name="tech" />
    <Radio label="Python" id="py" name="tech" />
  </RadioGroup>
);

# RadioGroup

> RadioGruop is Wrapper for component Radio to make them display flex and have to variant horizontal and vertical

## Example Usage

```tsx
import { RadioGroup } from '@zog-ui/core';
```

<Canvas columns={2} withSource="close">
  <Story
    args={{
      orientation: 'horizontal',
    }}
    name="RadioGroup"
  >
    {RadioGroupStory.bind()}
  </Story>
</Canvas>

<ArgsTable of={RadioGroup} />
