import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { BADGE } from '@geometricpanda/storybook-addon-badges';
import { within, userEvent } from '@storybook/testing-library';
import { expect } from '@storybook/jest';
import { ControlGroup } from '@zero-ui/core';

<Meta title="Components/Forms/ControlGroup" component={[ControlGroup]} argTypes={{}} />

export const ControlGroupStory = () => <ControlGroup />;

# ControlGroup

> WIP

## Example Usage

```jsx
import { ControlGroup } from '@zero-ui/core';
```

<Canvas columns={2} withSource="close">
  <Story name="ControlGroup">{ControlGroupStory.bind()}</Story>
</Canvas>

<ArgsTable of={ControlGroup} />
