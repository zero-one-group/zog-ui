import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { BADGE } from '@geometricpanda/storybook-addon-badges';
import { within, userEvent } from '@storybook/testing-library';
import { expect } from '@storybook/jest';
import { Switch } from '@zero-ui/core';

<Meta
  title="Components/Forms/Switch"
  component={[Switch]}
  argTypes={{
    size: {
      options: ['sm', 'md'],
      control: { type: 'radio' },
    },
    colorScheme: {
      options: ['pink', 'red', 'blue', 'teal'],
      control: { type: 'select' },
    },
  }}
/>

export const SwitchStory = (args) => <Switch {...args} />;

# Switch

> WIP

## Example Usage

```jsx
import { Switch } from '@zero-ui/core';
```

<Canvas columns={2} withSource="close">
  <Story
    name="Switch"
    args={{
      colorScheme: 'pink',
      size: 'md',
      disabled: false,
      loading: false,
    }}
  >
    {SwitchStory.bind()}
  </Story>
</Canvas>

<ArgsTable of={Switch} />
