import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { BADGE } from '@geometricpanda/storybook-addon-badges';
import { within, userEvent } from '@storybook/testing-library';
import { expect } from '@storybook/jest';
import { Slider, Space } from '@zero-ui/core';

<Meta title="Components/Forms/Slider" component={[Slider]} argTypes={{}} />

export const SliderStory = (args) => <Slider {...args} />;

# Slider

> WIP

## Example Usage

```jsx
import { Slider } from '@zero-ui/core';
```

<Canvas columns={2} withSource="close">
  <Story
    name="Horizontal"
    args={{
      disabled: false,
    }}
  >
    {SliderStory.bind()}
  </Story>
  <Story
    name="Vertical"
    args={{
      orientation: 'vertical',
      disabled: false,
    }}
  >
    {SliderStory.bind()}
  </Story>
</Canvas>

<ArgsTable of={Slider} />
