import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { BADGE } from '@geometricpanda/storybook-addon-badges';
import { within, userEvent } from '@storybook/testing-library';
import { expect } from '@storybook/jest';
import { TimePicker } from '@zero-ui/core';
import { useState } from 'react';

<Meta
  title="Components/Forms/TimePicker"
  component={[TimePicker]}
  argTypes={{
    size: {
      options: ['small', 'medium', 'large'],
      control: { type: 'radio' },
    },
  }}
/>

export const TimePickerStory = (args) => {
  const [value, setValue] = useState();
  console.log(value);
  return (
    <div style={{ height: '800px' }}>
      <h2>Uncontrolled</h2>
      <TimePicker {...args} />
      <h2>Controlled</h2>
      <TimePicker {...args} value={value} onChange={setValue} />
      <h2>OnChange Only</h2>
      <TimePicker {...args} onChange={setValue} />
      <h2>Listen Value Only</h2>
      <TimePicker {...args} value={value} />
    </div>
  );
};

# TimePicker

> By clicking the input box, you can select a time from a popup panel.
> Can be single time picker or time range picker

## Example Usage

```jsx
import { TimePicker } from '@zero-ui/core';
```

<Canvas columns={2} withSource="close">
  <Story
    name="TimePicker"
    args={{
      disabled: false,
      colorScheme: 'teal',
      isRange: true,
      placeholder: 'Select Time',
      placeholderStart: 'Select Start',
      placeholderEnd: 'Select End',
      size: 'medium',
    }}
  >
    {TimePickerStory.bind()}
  </Story>
</Canvas>

<ArgsTable of={TimePicker} />
