import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { BADGE } from '@geometricpanda/storybook-addon-badges';
import { within, userEvent } from '@storybook/testing-library';
import { expect } from '@storybook/jest';
import { SimpleToggle } from '@zero-ui/core';

<Meta title="Components/Forms/SimpleToggle" component={[SimpleToggle]} argTypes={{}} />

export const SimpleToggleStory = () => <SimpleToggle />;

# SimpleToggle

> WIP

## Example Usage

```jsx
import { SimpleToggle } from '@zero-ui/core';
```

<Canvas columns={2} withSource="close">
  <Story name="SimpleToggle">{SimpleToggleStory.bind()}</Story>
</Canvas>

<ArgsTable of={SimpleToggle} />
