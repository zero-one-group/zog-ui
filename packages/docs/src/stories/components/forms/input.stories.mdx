import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { Input } from '@zero-ui/core';

<Meta
  title="Components/Forms/Input"
  component={[Input]}
  argTypes={{
    size: {
      options: ['small', 'medium', 'large', 'fullWidth'],
      control: { type: 'radio' },
    },
    disabled: {
      control: 'boolean',
    },
  }}
/>

export const InputStory = ({ children, ...args }) => <Input {...args}></Input>;

# Input

> WIP

## Example Usage

```tsx
import { Input } from '@zero-ui/core';
```

<Canvas columns={2} withSource="close">
  <Story
    name="Input"
    args={{
      defaultValue: '',
      value: '',
      placeHolder: 'input text',
      maxLength: '',
      minLength: '',
    }}
  >
    {InputStory.bind()}
  </Story>
</Canvas>

<ArgsTable of={Input} />
