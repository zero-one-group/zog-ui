import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { Checkbox } from '@zero-ui/core';

<Meta
  title="Components/Forms/Checkbox"
  component={[Checkbox]}
  argTypes={{
    checked: {
      options: [true, false, 'indeterminated'],
      control: { type: 'radio' },
    },
    colorScheme: {
      defaultValue: '#1890FF',
      control: { type: 'text' },
    },
  }}
/>

export const CheckboxStory = ({ children, ...args }) => (
  <Checkbox {...args}>{children}</Checkbox>
);

# Checkbox

> Checkbox component used for selecting multiple values from several options.
> And you can easily customize and extends the Checkbox component with `css` prop and `labelCss` prop

## Example Usage

```jsx
import { Checkbox } from '@zero-ui/core';
```

<Canvas columns={2} withSource="close">
  <Story
    name="Checkbox"
    args={{
      disabled: false,
      boxSize: '$3',
      children: 'Label',
      labelCss: {
        fontFamily: '$mono',
      },
      css: {
        background: '$grayA2',
        borderRadius: 0,
      },
    }}
  >
    {CheckboxStory.bind()}
  </Story>
</Canvas>

<ArgsTable of={Checkbox} />
