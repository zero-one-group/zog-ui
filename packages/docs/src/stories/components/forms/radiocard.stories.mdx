import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { BADGE } from '@geometricpanda/storybook-addon-badges';
import { within, userEvent } from '@storybook/testing-library';
import { expect } from '@storybook/jest';
import { RadioCard } from '@zero-ui/core';

<Meta title="Components/Forms/RadioCard" component={[RadioCard]} argTypes={{}} />

export const RadioCardStory = () => <RadioCard />;

# RadioCard

> WIP

## Example Usage

```jsx
import { RadioCard } from '@zero-ui/core';
```

<Canvas columns={2} withSource="close">
  <Story name="RadioCard">{RadioCardStory.bind()}</Story>
</Canvas>

<ArgsTable of={RadioCard} />
