import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { InputPassword } from '@zero-ui/core';

<Meta
  title="Components/Forms/Input"
  component={[InputPassword]}
  argTypes={{
    size: {
      options: ['small', 'medium', 'large', 'fullWidth'],
      control: { type: 'radio' },
    },
  }}
/>

export const InputPasswordStory = ({ children, ...args }) => (
  <InputPassword {...args}></InputPassword>
);

# InputPassword

> WIP

## Example Usage

```tsx
import { InputPassword } from '@zero-ui/core';
```

<Canvas columns={2} withSource="close">
  <Story
    name="InputPassword"
    args={{
      defaultValue: '',
      value: '',
      placeHolder: 'input password',
    }}
  >
    {InputPasswordStory.bind()}
  </Story>
</Canvas>

<ArgsTable of={InputPassword} />
