import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { BADGE } from '@geometricpanda/storybook-addon-badges';
import { within, userEvent } from '@storybook/testing-library';
import { expect } from '@storybook/jest';
import {
  Button,
  Checkbox,
  DatePicker,
  DateRangePicker,
  FormItem,
  Input,
  InputNumber,
  InputPassword,
  Radio,
  RadioButton,
  RadioButtonGroup,
  RadioGroup,
  Select,
  Slider,
  Switch,
  TextArea,
  TimePicker,
} from '@zero-ui/core';

<Meta
  title="Components/Forms/Form/FormItem"
  component={[FormItem]}
  argTypes={{
    size: {
      options: ['sm', 'md', 'lg'],
      control: { type: 'radio' },
    },
    layout: {
      options: ['vertical', 'horizontal'],
      control: { type: 'radio' },
    },
  }}
/>

export const FormItemStory = (args) => (
  <div>
    <FormItem {...args}>
      <Select />
    </FormItem>
    <FormItem {...args}>
      <DatePicker />
    </FormItem>
    <FormItem {...args}>
      <DateRangePicker />
    </FormItem>
    <FormItem {...args}>
      <TimePicker />
    </FormItem>
    <FormItem {...args}>
      <Checkbox>Item01</Checkbox>
    </FormItem>
    <FormItem {...args}>
      <Switch />
    </FormItem>
    <FormItem {...args}>
      <TextArea css={{ width: '100%' }} />
    </FormItem>
    <FormItem {...args}>
      <RadioGroup>
        <Radio defaultChecked label="html" id="html" name="tech" />
        <Radio label="css" id="css" name="tech" />
        <Radio label="web" id="web" name="tech" />
        <Radio label="javascript" id="js" name="tech" />
        <Radio label="Python" id="py" name="tech" />
      </RadioGroup>
    </FormItem>
    <FormItem {...args}>
      <RadioButtonGroup>
        <RadioButton defaultChecked label="html" id="b-html" name="tech" />
        <RadioButton label="css" id="b-css" name="tech" />
        <RadioButton label="web" id="b-web" name="tech" />
        <RadioButton label="javascript" id="b-js" name="tech" />
        <RadioButton label="Python" id="b-py" name="tech" />
      </RadioButtonGroup>
    </FormItem>
    <FormItem {...args}>
      <Slider />
    </FormItem>
    <FormItem {...args}>
      <Input />
    </FormItem>
    <FormItem {...args}>
      <InputNumber />
    </FormItem>
    <FormItem {...args}>
      <InputPassword />
    </FormItem>
    <FormItem
      labelCol={args.labelCol}
      wrapperCol={args.wrapperCol}
      layout={args.layout}
      size={args.size}
      disabled={args.disabled}
    >
      <Button>Submit</Button>
    </FormItem>
  </div>
);

# FormItem

> Component to handle the form field layout, invalid or warning state. FormItem automatically send the state (isInvalid, isWarning, disabled) to the input component

## Example Usage

```tsx
import { FormItem } from '@zero-ui/core';
// import input components ...
```

<Canvas columns={2} withSource="close">
  <Story
    args={{
      label: 'Genre',
      required: false,
      disabled: false,
      labelCol: 5,
      wrapperCol: 12,
      isInvalid: false,
      isWarning: false,
      errorMessage: '',
      warningMessage: '',
      helpText: '',
      size: 'md',
    }}
    name="FormItem"
  >
    {FormItemStory.bind()}
  </Story>
</Canvas>

<ArgsTable of={FormItem} />
