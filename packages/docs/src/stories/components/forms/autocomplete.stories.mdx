import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { BADGE } from '@geometricpanda/storybook-addon-badges';
import { within, userEvent } from '@storybook/testing-library';
import { expect } from '@storybook/jest';
import { AutoComplete } from '@zog-ui/core';

<Meta
  title="Components/Forms/AutoComplete"
  component={[AutoComplete]}
  argTypes={{
    options: {
      control: { type: 'object' },
    },
  }}
/>

export const AutoCompleteStory = (args) => (
  <AutoComplete
    css={{
      width: '200px',
      px: '$2',
      py: '$1',
      fontSize: '12px',
    }}
    {...args}
  />
);

# AutoComplete

> WIP

## Example Usage

```jsx
import { AutoComplete } from '@zog-ui/core';
```

<Canvas columns={2} withSource="close">
  <Story
    name="Uncontrolled"
    args={{
      options: [
        'Angular',
        'React',
        'Vue',
        'Svelte',
        'Solid',
        'Qwik',
        'Node.js',
        'Next.js',
        'Remix',
        'Astro',
        'SvelteKit',
        'Nx',
      ],
    }}
  >
    {AutoCompleteStory.bind()}
  </Story>
  <Story
    name="Controlled"
    args={{
      options: ['Angular', 'React', 'Vue', 'Svelte', 'Solid', 'Qwik'],
      value: '',
    }}
  >
    {AutoCompleteStory.bind()}
  </Story>
</Canvas>

<ArgsTable of={AutoComplete} />
