import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { BADGE } from '@geometricpanda/storybook-addon-badges';
import { within, userEvent } from '@storybook/testing-library';
import { expect } from '@storybook/jest';
import { SearchBox } from '@zog-ui/core';
import { SettingOutlined } from '@ant-design/icons';

<Meta
  title="Components/Forms/SearchBox"
  component={[SearchBox]}
  argTypes={{
    size: {
      options: ['sm', 'md', 'lg', 'fullWidth'],
      control: { type: 'radio' },
    },
    intent: {
      options: ['default', 'secondary', 'primary', 'danger'],
      control: { type: 'radio' },
    },
    showCloseIcon: {
      control: 'boolean',
    },
    showVoiceIcon: {
      control: 'boolean',
    },
  }}
/>

export const SearchBoxStory = (args) => <SearchBox {...args} />;

# SearchBox

> WIP

## Example Usage

```jsx
import { SearchBox } from '@zog-ui/core';
```

<Canvas columns={2} withSource="close">
  <Story
    args={{
      defaultValue: '',
      placeHolder: 'search',
      value: '',
      label: 'Search',
      iconButton: <SettingOutlined />,
    }}
    name="SearchBox"
  >
    {SearchBoxStory.bind()}
  </Story>
</Canvas>

<ArgsTable of={SearchBox} />
