import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { BADGE } from '@geometricpanda/storybook-addon-badges';
import { within, userEvent } from '@storybook/testing-library';
import { expect } from '@storybook/jest';
import { RadioButton } from '@zero-ui/core';

<Meta title="Components/Forms/Radio" component={[RadioButton]} argTypes={{}} />

export const RadioButtonStory = (args) => <RadioButton {...args} />;

# RadioButton

> RadioButton is the variant's element input radio using the ui looks button

## Example Usage

```tsx
import { RadioButton } from '@zero-ui/core';
```

<Canvas columns={2} withSource="close">
  <Story
    args={{
      name: 'list',
      id: 'list-radio',
      label: 'html',
      disabled: false,
    }}
    name="RadioButton"
  >
    {RadioButtonStory.bind()}
  </Story>
</Canvas>

<ArgsTable of={RadioButton} />
