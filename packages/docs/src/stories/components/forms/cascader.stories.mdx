import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { BADGE } from '@geometricpanda/storybook-addon-badges';
import { within, userEvent } from '@storybook/testing-library';
import { expect } from '@storybook/jest';
import { Cascader } from '@zero-ui/core';

<Meta title="Components/Forms/Cascader" component={[Cascader]} argTypes={{}} />

export const CascaderStory = (args) => <Cascader {...args} />;

# Cascader

> WIP

## Example Usage

```jsx
import { Cascader } from '@zero-ui/core';
```

export const options = [
  {
    value: 'zhejiang',
    label: 'Zhejiang',
    children: [
      {
        value: 'hangzhou',
        label: 'Hangzhou',
        children: [
          {
            value: 'xihu',
            label: 'West Lake',
          },
        ],
      },
    ],
  },
  {
    value: 'jiangsu',
    label: 'Jiangsu',
    children: [
      {
        value: 'nanjing',
        label: 'Nanjing',
        children: [
          {
            value: 'zhonghuamen',
            label: 'Zhong Hua Men',
          },
        ],
      },
    ],
  },
];

<Canvas columns={2} withSource="close">
  <Story
    name="Cascader"
    args={{
      colorScheme: 'amber',
      options,
    }}
  >
    {CascaderStory.bind()}
  </Story>
</Canvas>

<ArgsTable of={Cascader} />
