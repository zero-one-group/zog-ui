import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { BADGE } from '@geometricpanda/storybook-addon-badges';
import { within, userEvent } from '@storybook/testing-library';
import { expect } from '@storybook/jest';
import { RadioGrid } from '@01group-ui/core';

<Meta title="Components/Forms/RadioGrid" component={[RadioGrid]} argTypes={{}} />

export const RadioGridStory = () => <RadioGrid />;

# RadioGrid

> WIP

## Example Usage

```jsx
import { RadioGrid } from '@01group-ui/core';
```

<Canvas columns={2} withSource="close">
  <Story name="RadioGrid">{RadioGridStory.bind()}</Story>
</Canvas>

<ArgsTable of={RadioGrid} />
