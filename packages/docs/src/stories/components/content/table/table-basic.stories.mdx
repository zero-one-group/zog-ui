import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { Table } from '@zog-ui/core';

<Meta
  title="Components/Content/Table/Basic"
  component={[Table]}
  argTypes={{
    size: {
      options: ['sm', 'md', 'lg'],
      control: { type: 'radio' },
    },
  }}
/>

export const elements = [
  { position: 6, mass: 12.011, symbol: 'C', name: 'Carbon' },
  { position: 7, mass: 14.007, symbol: 'N', name: 'Nitrogen' },
  { position: 39, mass: 88.906, symbol: 'Y', name: 'Yttrium' },
  { position: 56, mass: 137.33, symbol: 'Ba', name: 'Barium' },
  { position: 58, mass: 140.12, symbol: 'Ce', name: 'Cerium' },
];

export const TableBasic = (args) => (
  <Table {...args}>
    <thead>
      <tr>
        <th>Element position</th>
        <th>Element name</th>
        <th>Symbol</th>
        <th>Atomic mass</th>
      </tr>
    </thead>
    <tbody>
      {elements.map((element) => (
        <tr key={element.name}>
          <td>{element.position}</td>
          <td>{element.name}</td>
          <td>{element.symbol}</td>
          <td>{element.mass}</td>
        </tr>
      ))}
    </tbody>
  </Table>
);

# Table

> Render basic table with styles

## Example Usage

```jsx
import { Table } from '@zog-ui/core';

const App = () => {
  const elements = [
    { position: 6, mass: 12.011, symbol: 'C', name: 'Carbon' },
    { position: 7, mass: 14.007, symbol: 'N', name: 'Nitrogen' },
    { position: 39, mass: 88.906, symbol: 'Y', name: 'Yttrium' },
    { position: 56, mass: 137.33, symbol: 'Ba', name: 'Barium' },
    { position: 58, mass: 140.12, symbol: 'Ce', name: 'Cerium' },
  ];

  return (
    <Table>
      <thead>
        <tr>
          <th>Element position</th>
          <th>Element name</th>
          <th>Symbol</th>
          <th>Atomic mass</th>
        </tr>
      </thead>
      <tbody>
        {elements.map((element) => (
          <tr key={element.name}>
            <td>{element.position}</td>
            <td>{element.name}</td>
            <td>{element.symbol}</td>
            <td>{element.mass}</td>
          </tr>
        ))}
      </tbody>
    </Table>
  );
};
```

<Canvas columns={2} withSource="close">
  <Story
    name="Basic"
    args={{
      bordered: false,
      size: 'lg',
      striped: false,
    }}
  >
    {TableBasic.bind()}
  </Story>
</Canvas>

<ArgsTable of={Table} />
