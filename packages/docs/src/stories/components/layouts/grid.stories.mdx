import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { Grid, Box } from '@zero-ui/core';

<Meta title="Components/Layouts/Grid" component={[Grid]} argTypes={{}} />

export const GridStory = (args) => (
  <Grid {...args}>
    <Box css={{ backgroundColor: '$gray4', padding: '$4', borderRadius: '$2' }}>
      Box Left
    </Box>
    <Box css={{ backgroundColor: '$gray4', padding: '$4', borderRadius: '$2' }}>
      Box Right
    </Box>
  </Grid>
);

# Grid

Grid is a layout to handle element with `grid` layouts

## Example Usage

```jsx
import { Grid } from '@zero-ui/core';
```

<Canvas columns={2} withSource="close">
  <Story
    name="Grid"
    args={{
      templateColumns: '100px auto',
      columnGap: '$2',
    }}
  >
    {GridStory.bind()}
  </Story>
</Canvas>

<ArgsTable of={Grid} />
