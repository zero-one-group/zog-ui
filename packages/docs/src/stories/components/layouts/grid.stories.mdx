import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { Grid, Box } from '@zog-ui/core';

<Meta
  title="Components/Layouts/Grid"
  component={[Grid]}
  argTypes={{
    autoColumns: { control: 'text' },
    autoRows: { control: 'text' },
    autoFlow: {
      control: 'radio',
      options: ['row', 'column', 'dense', 'row dense', 'column dense'],
    },
  }}
/>

export const GridStory = (args) => (
  <Grid {...args}>
    <Box css={{ backgroundColor: '$gray4', padding: '$4', borderRadius: '$2' }}>
      1
    </Box>
    <Box css={{ backgroundColor: '$gray4', padding: '$4', borderRadius: '$2' }}>
      2
    </Box>
    <Box css={{ backgroundColor: '$gray4', padding: '$4', borderRadius: '$2' }}>
      3
    </Box>
    <Box css={{ backgroundColor: '$gray4', padding: '$4', borderRadius: '$2' }}>
      4
    </Box>
    <Box css={{ backgroundColor: '$gray4', padding: '$4', borderRadius: '$2' }}>
      5
    </Box>
    <Box css={{ backgroundColor: '$gray4', padding: '$4', borderRadius: '$2' }}>
      6
    </Box>
  </Grid>
);

# Grid

Grid is a layout to handle element with `grid` layouts

## Example Usage

```jsx
import { Grid } from '@zog-ui/core';
```

<Canvas columns={2} withSource="close">
  <Story
    name="Grid"
    args={{
      autoColumns: '',
      autoRows: '',
      autoFlow: 'row',
      templateColumns: 'auto 100px auto',
      templateRows: 'auto 150px',
      columnGap: '',
      rowGap: '',
      gap: '$2',
    }}
  >
    {GridStory.bind()}
  </Story>
</Canvas>

<ArgsTable of={Grid} />
