import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { PageHeader, Button, Space, Breadcrumb } from '@01group-ui/core';
import { LeftOutlined } from '@ant-design/icons';

<Meta
  title="Components/Layouts/PageHeader"
  component={[PageHeader]}
  argTypes={{
    title: {
      description: 'Page Title <br/>`ReactNode`',
      control: { type: 'text' },
    },
    subtitle: {
      description: 'Page Subtitle <br/>`ReactNode`',
      control: { type: 'text' },
    },
    extra: {
      description: 'Extra Heading on the right side <br/>`ReactNode`',
    },
    avatar: {
      description: '`AvatarProps`',
    },
    breadcrumb: {
      description: '`BreadcrumbProps`',
    },
    backIcon: {
      description: 'Custom back icon <br/>`ReactNode`',
    },
    children: {
      description: '`ReactNode`',
    },
    onBack: {
      description: 'Function called when icon back clicked <br/>`()=>void`',
    },
    css: {
      description: 'Customize CSS <br/>`CSS`',
    },
  }}
/>

export const PageHeaderStory = (args) => (
  <PageHeader {...args}>{args.children}</PageHeader>
);

# PageHeader

> A header with common actions and design elements built in.

## Example Usage

```jsx
import { Button, PageHeader, Space } from '@01group-ui/core';
import { LeftOutlined } from '@ant-design/icons';
```

<Canvas columns={2} withSource="close">
  <Story
    name="PageHeader"
    args={{
      title: 'Title',
      subtitle: 'Subtitle',
      extra: (
        <Space gap="s">
          <Button variant="default" size="md">
            Default
          </Button>
          <Button variant="default" size="md">
            Default
          </Button>
          <Button variant="dashed" size="md">
            Dashed
          </Button>
        </Space>
      ),
      onBack: () => {},
      backIcon: <LeftOutlined />,
      breadcrumb: {
        items: [
          { title: 'Mantine', href: '#', current: false },
          { title: 'Mantine hooks', href: '#', current: false },
          { title: 'use-id', href: '#', current: true },
        ],
        separator: '/',
      },
      avatar: {
        src: 'https://avatars.githubusercontent.com/u/921834?v=4&s=200',
      },
      children: (
        <>
          <b>Page Header Content</b>
          <br />
          <i>Page Header Children</i>
        </>
      ),
      css: {
        border: '1px solid whitesmoke',
      },
    }}
  >
    {PageHeaderStory.bind()}
  </Story>
</Canvas>

<ArgsTable story="PageHeader" />
