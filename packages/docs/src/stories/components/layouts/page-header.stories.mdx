import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import {
  PageHeader,
  PageHeaderProps,
  Button,
  Space,
  Breadcrumb,
} from '@zero-ui/core';
import { LeftOutlined } from '@ant-design/icons';

<Meta
  title="Components/Layouts/PageHeader"
  component={[PageHeader]}
  argTypes={{
    title: {
      description: 'Page Title',
      control: { type: 'text' },
    },
    subtitle: {
      description: 'Page Subtitle',
      control: { type: 'text' },
    },
    extra: {
      name: 'Extra',
      description: 'Extra Heading',
    },
    avatar: {
      description: 'AvatarProps',
    },
    breadcrumb: {
      description: 'BreadcrumbProps',
    },
  }}
/>

export const PageHeaderStory = (args) => (
  <PageHeader {...args}>{args.children}</PageHeader>
);

# PageHeader

Box is an essential component used in the most of Zero UI components. Box doesn't have unique props, you can easily customize and extends the Box component with `css` prop or `as` prop.

## Example Usage

```jsx
import { Button, PageHeader, Space } from '@zero-ui/core';
import { LeftOutlined } from '@ant-design/icons';
```

<Canvas columns={2} withSource="close">
  <Story
    name="PageHeader"
    args={{
      title: 'Title',
      subtitle: 'Subtitle',
      extra: (
        <Space gap="s">
          <Button variant="default" size="md">
            Default
          </Button>
          <Button variant="default" size="md">
            Default
          </Button>
          <Button variant="dashed" size="md">
            Dashed
          </Button>
        </Space>
      ),
      onBack: () => {},
      backIcon: <LeftOutlined />,
      breadcrumb: {
        items: [
          { title: 'Mantine', href: '#', current: false },
          { title: 'Mantine hooks', href: '#', current: false },
          { title: 'use-id', href: '#', current: true },
        ],
        separator: '/',
      },
      avatar: {
        src: 'https://avatars.githubusercontent.com/u/921834?v=4&s=200',
      },
      children: (
        <>
          <b>Page Header Content</b>
          <br />
          <i>Page Header Children</i>
        </>
      ),
      css: {
        border: '1px solid whitesmoke',
      },
    }}
  >
    {PageHeaderStory.bind()}
  </Story>
</Canvas>

<ArgsTable story="PageHeader" />
