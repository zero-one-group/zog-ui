import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { BADGE } from '@geometricpanda/storybook-addon-badges';
import { within, userEvent } from '@storybook/testing-library';
import { expect } from '@storybook/jest';
import { Layout, Box } from '@zero-ui/core';

<Meta title="Components/Layouts/Layout" component={[Layout]} argTypes={{}} />

# Layout

Wrapper component for handling the overall layout of a page.

## Example Usage

```tsx
import { Layout } from '@zero-ui/core';
```

<Canvas columns={2} withSource="close">
  <Story name="Basic Layout">
    {() => (
      <Layout>
        <Box
          css={{
            backgroundColor: '$blue4',
            padding: '$4',
          }}
        >
          Header
        </Box>
        <Box
          css={{
            backgroundColor: '$blue6',
            padding: '$4',
          }}
        >
          Content
        </Box>
        <Box
          css={{
            backgroundColor: '$blue4',
            padding: '$4',
          }}
        >
          Footer
        </Box>
      </Layout>
    )}
  </Story>
  <Story name="Sider Layout">
    {() => (
      <>
        <Layout>
          <Box
            css={{
              backgroundColor: '$blue4',
              padding: '$4',
            }}
          >
            Header
          </Box>
          <Layout hasSider>
            <Box
              css={{
                backgroundColor: '$blue5',
                padding: '$4',
                width: '200px',
              }}
            >
              Sider
            </Box>
            <Box
              css={{
                backgroundColor: '$blue6',
                padding: '$4',
                flex: 'auto',
              }}
            >
              Content
            </Box>
          </Layout>
          <Box
            css={{
              backgroundColor: '$blue4',
              padding: '$4',
            }}
          >
            Footer
          </Box>
        </Layout>
        <Layout css={{ mt: '$4' }}>
          <Box
            css={{
              backgroundColor: '$blue4',
              padding: '$4',
            }}
          >
            Header
          </Box>
          <Layout hasSider>
            <Box
              css={{
                backgroundColor: '$blue6',
                padding: '$4',
                flex: 'auto',
              }}
            >
              Content
            </Box>
            <Box
              css={{
                backgroundColor: '$blue5',
                padding: '$4',
                width: '200px',
              }}
            >
              Sider
            </Box>
          </Layout>
          <Box
            css={{
              backgroundColor: '$blue4',
              padding: '$4',
            }}
          >
            Footer
          </Box>
        </Layout>
      </>
    )}
  </Story>
</Canvas>
