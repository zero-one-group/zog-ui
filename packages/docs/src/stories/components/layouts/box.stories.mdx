import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { Box, BoxProps } from '@zog-ui/core';

<Meta title="Components/Layouts/Box" component={[Box]} argTypes={{}} />

export const BoxStory = (args) => (
  <Box {...args}>
    <p>{args.children}</p>
  </Box>
);

# Box

Box is an essential component used in the most of Zero One Group UI components. Box doesn't have unique props, you can easily customize and extends the Box component with `css` prop or `as` prop.

## Example Usage

```jsx
import { Box } from '@zog-ui/core';
```

<Canvas columns={2} withSource="close">
  <Story
    name="Box"
    args={{
      children: 'Box',
      asChild: false,
      css: {
        backgroundColor: '$gray4',
        padding: '$4',
        borderRadius: '$2',
      },
    }}
  >
    {BoxStory.bind()}
  </Story>
</Canvas>

<ArgsTable story="Box" />
