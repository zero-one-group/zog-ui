import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { Box, BoxProps } from '@zero-ui/core';

<Meta title="Components/Layouts/Box" component={[Box]} argTypes={{}} />

export const BoxStory = (args) => <Box {...args}>{args.children}</Box>;

# Box

Box is a basic component that used in the most of Zero UI components. Box doesn't have a special props, you can easily customize and extends Box component with `css` prop or `as` prop.

## Example Usage

```jsx
import { Box } from '@zero-ui/core';
```

<Canvas columns={2} withSource="close">
  <Story
    name="Box"
    args={{
      children: 'Box',
      css: {
        backgroundColor: '$gray4',
        padding: '$4',
        borderRadius: '$2',
      },
    }}
  >
    {BoxStory.bind()}
  </Story>
</Canvas>

<ArgsTable story="Box" />
