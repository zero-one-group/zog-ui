import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { BADGE } from '@geometricpanda/storybook-addon-badges';
import { within, userEvent } from '@storybook/testing-library';
import { expect } from '@storybook/jest';
import { Space, Box } from '@zero-ui/core';

<Meta
  title="Components/Layouts/Space"
  component={[Space]}
  argTypes={{
    gap: {
      options: ['s', 'm', 'l', 'xl', 'xxl'],
      control: { type: 'radio' },
    },
    direction: {
      options: ['row', 'row-reverse', 'column', 'column-reverse'],
      control: { type: 'radio' },
    },
    wrap: {
      options: ['nowrap', 'wrap', 'wrap-reverse'],
      control: { type: 'radio' },
    },
    justify: {
      options: [
        'start',
        'end',
        'flex-start',
        'flex-end',
        'center',
        'left',
        'right',
        'normal',
        'space-between',
        'space-around',
        'space-evenly',
        'stretch',
      ],
      control: { type: 'select' },
    },
    align: {
      options: [
        'start',
        'end',
        'flex-start',
        'flex-end',
        'center',
        'self-start',
        'self-end',
        'stretch',
        'safe',
        'unsafe',
      ],
      control: { type: 'select' },
    },
  }}
/>

export const SpaceStory = (args) => (
  <Space {...args}>
    <Box css={{ backgroundColor: '$gray4', padding: '$4', borderRadius: '$2' }}>
      Box 1
    </Box>
    <Box css={{ backgroundColor: '$gray4', padding: '$4', borderRadius: '$2' }}>
      Box 2
    </Box>
    <Box
      css={{
        backgroundColor: '$gray4',
        height: '$7',
        padding: '$4',
        borderRadius: '$2',
      }}
    >
      Box 3
    </Box>
    <Box
      css={{
        backgroundColor: '$gray4',
        height: '$7',
        padding: '$4',
        borderRadius: '$2',
      }}
    >
      Box 4
    </Box>
    <Box css={{ backgroundColor: '$gray4', padding: '$4', borderRadius: '$2' }}>
      Box 5
    </Box>
    <Box css={{ backgroundColor: '$gray4', padding: '$4', borderRadius: '$2' }}>
      Box 6
    </Box>
    <Box css={{ backgroundColor: '$gray4', padding: '$4', borderRadius: '$2' }}>
      Box 7
    </Box>
    <Box css={{ backgroundColor: '$gray4', padding: '$4', borderRadius: '$2' }}>
      Box 8
    </Box>
    <Box css={{ backgroundColor: '$gray4', padding: '$4', borderRadius: '$2' }}>
      Box 9
    </Box>
    <Box css={{ backgroundColor: '$gray4', padding: '$4', borderRadius: '$2' }}>
      Box 10
    </Box>
    <Box css={{ backgroundColor: '$gray4', padding: '$4', borderRadius: '$2' }}>
      Box 11
    </Box>
    <Box css={{ backgroundColor: '$gray4', padding: '$4', borderRadius: '$2' }}>
      Box 12
    </Box>
  </Space>
);

# Space

Space is a layout to handle element with `flex` layouts

## Example Usage

```jsx
import { Space } from '@zero-ui/core';
```

<Canvas columns={2} withSource="close">
  <Story
    name="Space"
    args={{
      direction: 'row',
      wrap: 'wrap',
      justify: 'flex-start',
      align: 'flex-start',
    }}
  >
    {SpaceStory.bind()}
  </Story>
</Canvas>

<ArgsTable of={Space} />
