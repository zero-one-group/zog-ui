import { Canvas, Meta, Primary } from '@storybook/blocks';
import * as Stories from './alert.stories';

<Meta of={Stories} />

# Alert

Displays a callout for user attention with different severity levels.

## Example

```tsx
import { Alert, AlertTitle, AlertIcon, AlertDescription } from '#/components/base-ui'
import * as Lucide from 'lucide-react'

// Basic usage
<Alert>
  <AlertIcon asChild>
    <Lucide.Info className="size-4" />
  </AlertIcon>
  <AlertTitle>Heads up!</AlertTitle>
  <AlertDescription>
    This is an important message.
  </AlertDescription>
</Alert>

// Destructive variant
<Alert variant="destructive">
  <AlertIcon asChild>
    <Lucide.Info className="size-4" />
  </AlertIcon>
  <AlertTitle>Error</AlertTitle>
  <AlertDescription>
    Something went wrong!
  </AlertDescription>
</Alert>
```

## Preview

<Primary />
