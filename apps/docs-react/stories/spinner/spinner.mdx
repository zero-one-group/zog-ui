import { Canvas, Meta, Primary } from '@storybook/blocks'
import * as Stories from './spinner.stories'

<Meta of={Stories} />

# Spinner

Rotating loading indicator to provide visual feedback to the user.

## Example

```tsx
import { LoadingSpinner, LazyLoadingSpinner } from '#/components/base-ui'

// Basic spinner
<LoadingSpinner />

// Lazy loading spinner with delay
<LazyLoadingSpinner delay={1000} />

// Custom lazy loading spinner
<LazyLoadingSpinner
  delay={1500}
  element={<LoadingSpinner />}
  fallback={<div>Loading...</div>}
/>
```

## Preview

<Primary />
